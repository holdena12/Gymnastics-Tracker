{
  "name": "gymnastics-skills-tracker",
  "version": "1.0.0",
  "description": "A comprehensive gymnastics skills tracking application with cross-device synchronization",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000 --directory src",
    "dev": "python3 -m http.server 8001 --directory src",
    "build": "echo 'No build step required for this client-side app'",
    "install-firebase": "npm install firebase",
    "setup": "npm run install-firebase && echo 'Setup complete! Configure Firebase settings in firebase-config.js'"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {},
  "keywords": [
    "gymnastics",
    "skills-tracker",
    "firebase",
    "cross-device",
    "progressive-web-app",
    "mobile-optimized"
  ],
  "author": "Gymnastics Skills Tracker",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/holdena12/Gymnastics-Tracker.git"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
