{
  "name": "gymnastics-tracker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npx http-server src --port 8080 --cors",
    "build": "mkdir -p dist && cp -r src/* dist/",
    "preview": "npx http-server dist --port 8080",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "http-server": "^14.1.1"
  },
  "dependencies": {}
}
